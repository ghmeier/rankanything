// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tiers.sql

package db

import (
	"context"
)

const createRankingTier = `-- name: CreateRankingTier :one
INSERT INTO ranking_tiers (ranking_version_id, ranking_id, title, color_hex, position)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, created_at, updated_at, position, ranking_version_id, ranking_id, title, color_hex
`

type CreateRankingTierParams struct {
	RankingVersionID int64
	RankingID        int64
	Title            string
	ColorHex         string
	Position         int16
}

func (q *Queries) CreateRankingTier(ctx context.Context, arg CreateRankingTierParams) (RankingTier, error) {
	row := q.db.QueryRow(ctx, createRankingTier,
		arg.RankingVersionID,
		arg.RankingID,
		arg.Title,
		arg.ColorHex,
		arg.Position,
	)
	var i RankingTier
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Position,
		&i.RankingVersionID,
		&i.RankingID,
		&i.Title,
		&i.ColorHex,
	)
	return i, err
}

const deleteRankingTier = `-- name: DeleteRankingTier :exec
DELETE FROM ranking_tiers WHERE id = $1
`

func (q *Queries) DeleteRankingTier(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, deleteRankingTier, id)
	return err
}

const getRankingTier = `-- name: GetRankingTier :one
SELECT id, created_at, updated_at, position, ranking_version_id, ranking_id, title, color_hex FROM ranking_tiers WHERE id = $1
`

func (q *Queries) GetRankingTier(ctx context.Context, id int64) (RankingTier, error) {
	row := q.db.QueryRow(ctx, getRankingTier, id)
	var i RankingTier
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Position,
		&i.RankingVersionID,
		&i.RankingID,
		&i.Title,
		&i.ColorHex,
	)
	return i, err
}

const listRankingTiersForVersion = `-- name: ListRankingTiersForVersion :many
SELECT id, created_at, updated_at, position, ranking_version_id, ranking_id, title, color_hex FROM ranking_tiers WHERE ranking_version_id = $1 ORDER BY position
`

func (q *Queries) ListRankingTiersForVersion(ctx context.Context, rankingVersionID int64) ([]RankingTier, error) {
	rows, err := q.db.Query(ctx, listRankingTiersForVersion, rankingVersionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []RankingTier
	for rows.Next() {
		var i RankingTier
		if err := rows.Scan(
			&i.ID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Position,
			&i.RankingVersionID,
			&i.RankingID,
			&i.Title,
			&i.ColorHex,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const nextRankingTierPosition = `-- name: NextRankingTierPosition :one
SELECT coalesce(max(position) + 1, 0)::smallint FROM ranking_tiers WHERE ranking_version_id = $1
`

func (q *Queries) NextRankingTierPosition(ctx context.Context, rankingVersionID int64) (int16, error) {
	row := q.db.QueryRow(ctx, nextRankingTierPosition, rankingVersionID)
	var column_1 int16
	err := row.Scan(&column_1)
	return column_1, err
}

const updateRankingTier = `-- name: UpdateRankingTier :one
UPDATE ranking_tiers
SET title = $2, color_hex = $3, position = $4, updated_at = now()
WHERE id = $1
RETURNING id, created_at, updated_at, position, ranking_version_id, ranking_id, title, color_hex
`

type UpdateRankingTierParams struct {
	ID       int64
	Title    string
	ColorHex string
	Position int16
}

func (q *Queries) UpdateRankingTier(ctx context.Context, arg UpdateRankingTierParams) (RankingTier, error) {
	row := q.db.QueryRow(ctx, updateRankingTier,
		arg.ID,
		arg.Title,
		arg.ColorHex,
		arg.Position,
	)
	var i RankingTier
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Position,
		&i.RankingVersionID,
		&i.RankingID,
		&i.Title,
		&i.ColorHex,
	)
	return i, err
}
